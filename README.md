# 学籍番号 OCR システム
#### 開発背景：デジタルとアナログのジレンマ
大学の出席管理において、ICカードリーダーによる自動化は便利ですが、カードだけをタッチして退出する「ピ逃げ」が課題となっています。これを防ぐために「アナログな紙の出席表」を併用する教員は少なくありませんが、そこには2つの大きな壁が存在します。
- `データ化の負担`: 大量の手書き書類を一つずつ入力する膨大な作業工数。
- `誤認識のリスク`: 完全自動のOCRでは、AIの誤認識が学生の成績に直結する不安。

#### 解決策：ローカルVLM × 人間による最終確認
本プロジェクトは、ローカルVLM（Vision Language Model）による推論と、人間によるクイックな確認を組み合わせた「半自動」のデータ化アプリケーションです。
- `プライバシーに配慮`: ローカル環境で推論を行うため、学外へのデータ流出を防ぎます。
- `確実なデータ化`: VLMが読み取った結果を人間が最後にチェックすることで、AI特有の誤認識リスクを排除します。
- `シームレスな体験`: Webカメラで撮影するだけで、即座に認識・保存フローへと繋げます。

#### デモ

https://github.com/user-attachments/assets/f1e045ac-161e-4aee-8b7e-ba991b5688b6

## セットアップ

### 1. Python パッケージのインストール

```bash
pip install fastapi uvicorn openai python-multipart pillow
```

### 2. LM Studio の起動

- LM Studio を起動し、`qwen/qwen2.5-vl-7b-instruct`（またはVLM対応モデル）をロード
- Local Server を `http://localhost:1234` で起動

### 3. バックエンドの起動

```bash
uvicorn backend:app --reload --port 8000
```

### 4. フロントエンドを開く

 `index.html` をクリックしブラウザで開いてください。

## 操作方法

| 操作 | 動作 |
|------|------|
| 「カメラを起動」ボタン または `Space` | カメラ起動 |
| マウスドラッグ | 学籍番号のROI（範囲）を選択 → 自動送信 |
| `Enter` | 認識結果を確定・リストへ追加 |
| `Space`（確認中） | やり直し（ROI再選択） |
| 「保存して終了」ボタン | `results.txt` に書き出し |

## API エンドポイント

| エンドポイント | 説明 |
|---------------|------|
| `GET /api/health` | LM Studio 接続確認 |
| `POST /api/recognize` | ROI画像を送信して学籍番号を取得 |
| `POST /api/save` | リストを `results.txt` に保存 |
